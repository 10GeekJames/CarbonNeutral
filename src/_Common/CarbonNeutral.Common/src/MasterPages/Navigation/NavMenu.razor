<MudNavMenu>
    <MudNavLink Href="/wsk/allgames" Match="NavLinkMatch.All" Icon="@Icons.Material.Filled.LibraryBooks">All Games</MudNavLink>
    <MudNavLink Href="/wsk/DevPlayGround" Match="NavLinkMatch.All" Icon="@Icons.Custom.Brands.GitHub">Dev Playground</MudNavLink>
</MudNavMenu>

@code {
    @* private bool collapseNavMenu = true;

    private string? NavMenuCssClass => collapseNavMenu ? "collapse" : null;

    private void ToggleNavMenu()
    {
        collapseNavMenu = !collapseNavMenu;
    } *@
}
