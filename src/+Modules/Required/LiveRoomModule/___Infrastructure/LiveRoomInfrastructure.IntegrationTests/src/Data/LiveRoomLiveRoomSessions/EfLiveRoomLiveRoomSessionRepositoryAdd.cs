namespace LiveRoomInfrastructure.IntegrationTests.Data.Books;
public class EfLiveRoomSessionRepositoryAdd : BaseTestFixture
{
   /*  [Fact]
    public async Task AddsBookSetsId()
    {
        var repository = BookRepository();
        var Book = BookLiveRoomTestData.BookAlfradoTheGreat;

        await repository.AddAsync(Book);

        var newBook = (await repository.ListAsync())
                        .FirstOrDefault();

        Assert.NotNull(newBook?.Id);
    } */
}
